import React from 'react'

export default function UpdatePostMain() {
  return (//Display list of all Blog Posts Available + filter options - Use Get all Posts API route + reroute to /update-post/:slug
    <div>UpdatePostMain</div>
  )
}
